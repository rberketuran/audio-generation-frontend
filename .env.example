# Backend API Base URL
# For local development, this can be left empty (defaults to http://localhost:8000)
# For production, set this to your backend API URL (e.g., https://your-backend.vercel.app)
# Note: Vite requires the VITE_ prefix for environment variables to be exposed to client code
VITE_API_BASE_URL=
